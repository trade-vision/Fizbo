setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: phzbo
build:
  docker:
    web: Phzbo-server/Dockerfile
  config: 
    FOO: bar
release:
  command:
    - ./deployment-tasks.sh
  image: phzbo
run:
  web: bundle exec puma -C config/puma.rb